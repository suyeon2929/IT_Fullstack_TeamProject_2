<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/nav_graph"
    app:startDestination="@id/homeFragment">

    <fragment
        android:id="@+id/homeFragment"
        android:name="com.bitc.plumMarket.Fragment.HomeFragment"
        android:label="Home Fragment">
        <!-- Other configurations for the home fragment -->
    </fragment>

    <fragment
        android:id="@+id/chatFragment"
        android:name="com.bitc.plumMarket.Fragment.ChatFragment"
        android:label="chat Fragment">
        <!-- Other configurations for the other fragment -->
    </fragment>

    <fragment
        android:id="@+id/myFragment"
        android:name="com.bitc.plumMarket.Fragment.MyFragment"
        android:label="my Fragment">
        <!-- Other configurations for the other fragment -->
    </fragment>

</navigation>